# yaml-language-server: $schema=https://schema.skills17.ch/task-config/v3/config.schema.json
id: local-history
serve:
  enabled: true
  bind: localhost
  port: 3000
localHistory: true
groups:
  - match: Local History.+
