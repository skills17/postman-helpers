# yaml-language-server: $schema=https://schema.skills17.ch/task-config/v3/config.schema.json
id: local-history-disabled
serve:
  enabled: true
  bind: localhost
  port: 3000
groups:
  - match: Local History.+
