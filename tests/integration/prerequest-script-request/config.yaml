# yaml-language-server: $schema=https://schema.skills17.ch/task-config/v3/config.schema.json
id: prerequest-script
serve:
  enabled: true
  bind: localhost
  port: 3000
groups:
  - match: Pre-request script with request.+
